import React from 'react';
import { useNavigate } from 'react-router-dom';
import { Box, Typography, IconButton, Tooltip } from '@mui/material';
import { Storefront, Home, Login } from '@mui/icons-material';
import { colors } from '../theme';

const Footer = () => {
  const navigate = useNavigate();

  return (
    <Box
      component="footer"
      sx={{
        background: 'linear-gradient(135deg, #0F172A 0%, #1E293B 100%)',
        color: 'white',
        py: 4,
        mt: 'auto',
        position: 'relative',
        overflow: 'hidden',
        borderTop: '1px solid rgba(255,255,255,0.08)',
      }}
    >
      <Box
        sx={{
          position: 'absolute',
          top: -60,
          right: -60,
          width: 200,
          height: 200,
          background: 'radial-gradient(circle, rgba(13,148,136,0.12) 0%, transparent 70%)',
          borderRadius: '50%',
          pointerEvents: 'none',
        }}
      />
      <Box
        sx={{
          position: 'absolute',
          bottom: -80,
          left: -80,
          width: 250,
          height: 250,
          background: 'radial-gradient(circle, rgba(81,149,151,0.08) 0%, transparent 70%)',
          borderRadius: '50%',
          pointerEvents: 'none',
        }}
      />

      <Box
        sx={{
          maxWidth: 1200,
          mx: 'auto',
          px: 3,
          display: 'flex',
          flexDirection: { xs: 'column', sm: 'row' },
          alignItems: 'center',
          justifyContent: 'space-between',
          gap: 2,
          position: 'relative',
        }}
      >
        <Box sx={{ display: 'flex', alignItems: 'center', gap: 1.5 }}>
          <Box
            sx={{
              bgcolor: 'rgba(255,255,255,0.1)',
              borderRadius: 2,
              p: 1,
              display: 'flex',
              transition: 'transform 0.2s ease',
              '&:hover': { transform: 'scale(1.1)' },
            }}
          >
            <Storefront sx={{ color: '#5EEAD4', fontSize: 28 }} />
          </Box>
          <Box>
            <Typography variant="subtitle1" fontWeight={700} sx={{ lineHeight: 1.2 }}>
              StoreHub
            </Typography>
            <Typography variant="caption" sx={{ color: 'rgba(255,255,255,0.6)' }}>
              Sari-Sari Store Stock Management
            </Typography>
          </Box>
        </Box>

        <Box sx={{ display: 'flex', alignItems: 'center', gap: 1 }}>
          <Tooltip title="Homepage">
            <IconButton
              onClick={() => navigate('/')}
              sx={{
                color: 'rgba(255,255,255,0.7)',
                transition: 'all 0.2s ease',
                '&:hover': {
                  color: '#5EEAD4',
                  transform: 'translateY(-2px)',
                  bgcolor: 'rgba(255,255,255,0.1)',
                },
                '&:active': { transform: 'scale(0.9)' },
              }}
            >
              <Home fontSize="small" />
            </IconButton>
          </Tooltip>
          <Tooltip title="Login">
            <IconButton
              onClick={() => navigate('/login')}
              sx={{
                color: 'rgba(255,255,255,0.7)',
                transition: 'all 0.2s ease',
                '&:hover': {
                  color: '#5EEAD4',
                  transform: 'translateY(-2px)',
                  bgcolor: 'rgba(255,255,255,0.1)',
                },
                '&:active': { transform: 'scale(0.9)' },
              }}
            >
              <Login fontSize="small" />
            </IconButton>
          </Tooltip>
        </Box>

        <Typography
          variant="body2"
          sx={{ color: 'rgba(255,255,255,0.6)', textAlign: { xs: 'center', sm: 'right' } }}
        >
          &copy; {new Date().getFullYear()} StoreHub. All rights reserved.
          <br />
          <span style={{ fontSize: '0.75rem' }}>
            Made with <span style={{ color: colors.red }}>♥</span> for your everyday needs
          </span>
        </Typography>
      </Box>
    </Box>
  );
};

export default Footer;